# Screenshots

Drop 4 images here to fill the grid in the root README:

- `01-assistant.png`  — the Assistant (COMMS) answering with rendered flights
- `02-search.png`     — the Search readout rows with price gauges
- `03-fares.png`      — the Fare Track heat calendar
- `04-command-bar.png`— the command bar / annunciator strip

Reference captures from earlier runs are available locally (gitignored) under
`docs/design/screens/` if you want to reuse them.
